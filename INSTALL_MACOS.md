# Инструкция по установке XeusGUI на macOS

Если при попытке запустить приложение вы видите ошибку **"the application can't be opened"** или **"XeusGUI cannot be opened because it is from an unidentified developer"**, следуйте этим инструкциям.

## 🔧 Быстрое решение

### Способ 1: Через терминал (рекомендуется)

1. Откройте **Терминал** (найдите в Программах → Утилиты)

2. Перейдите в папку с приложением:
   ```bash
   cd ~/Downloads  # или путь, куда вы скачали приложение
   ```

3. Удалите карантин и исправьте права:
   ```bash
   xattr -dr com.apple.quarantine XeusGUI.app
   chmod +x XeusGUI.app/Contents/MacOS/XeusGUI
   ```

4. Теперь запустите приложение:
   ```bash
   open XeusGUI.app
   ```

### Способ 2: Использование скрипта

Если в папке с приложением есть файл `fix_macos_permissions.sh`:

1. Откройте **Терминал**
2. Перейдите в папку с приложением
3. Запустите скрипт:
   ```bash
   ./fix_macos_permissions.sh
   ```

### Способ 3: Через Finder (без терминала)

1. Найдите файл `XeusGUI.app` в Finder
2. **Правый клик** (или Control + клик) на приложении
3. Выберите **"Открыть"** из контекстного меню
4. В появившемся окне нажмите **"Открыть"**

После первого запуска через "Открыть" macOS запомнит разрешение, и в дальнейшем можно будет запускать двойным кликом.

### Способ 4: Через Системные настройки

Если способ 3 не помог:

1. Попробуйте запустить приложение двойным кликом
2. Перейдите в **Системные настройки** → **Безопасность и конфиденциальность**
3. В разделе **"Общие"** вы увидите сообщение о заблокированном приложении
4. Нажмите кнопку **"Открыть в любом случае"** рядом с предупреждением
5. Подтвердите действие

## 📋 Подробное объяснение

### Почему это происходит?

macOS имеет систему безопасности **Gatekeeper**, которая блокирует запуск приложений от неизвестных разработчиков. Когда вы скачиваете приложение из интернета, macOS добавляет к нему атрибут **quarantine** (карантин), который блокирует запуск.

### Что делает каждый способ?

- **Способ 1 и 2** - удаляют атрибут карантина через терминал
- **Способ 3** - позволяет macOS добавить исключение для этого конкретного приложения
- **Способ 4** - вручную разрешает запуск через системные настройки

## ⚠️ Важные замечания

1. **Безопасность**: Убедитесь, что вы скачали приложение из надежного источника (официальный репозиторий GitHub)

2. **Права доступа**: После исправления прав приложение должно запускаться нормально

3. **Повторная установка**: Если вы переместите приложение в другую папку, может потребоваться повторить процедуру

## 🐛 Если ничего не помогает

1. Проверьте версию macOS:
   ```bash
   sw_vers
   ```
   Требуется macOS 10.13 или новее

2. Проверьте архитектуру процессора:
   ```bash
   uname -m
   ```
   Приложение собрано для arm64 (Apple Silicon) или x86_64 (Intel)

3. Попробуйте запустить через терминал для просмотра ошибок:
   ```bash
   ./XeusGUI.app/Contents/MacOS/XeusGUI
   ```

4. Убедитесь, что приложение не повреждено - проверьте его размер (должно быть около 500 MB)

## 📞 Получение помощи

Если проблема сохраняется:
1. Создайте [Issue на GitHub](https://github.com/pebenifi/xeus/issues)
2. Приложите вывод команды:
   ```bash
   ./XeusGUI.app/Contents/MacOS/XeusGUI
   ```

